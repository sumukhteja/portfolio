{
  "name": "sumukhteja-portfolio-static",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/static dist/content && cp index.html _redirects dist/ && cp -R static/* dist/static/ && cp -R content/ dist/content/"
  }
}

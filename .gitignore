# Real config — the .env inside content/ is sample text for the editor and stays.
/.env
.venv/
__pycache__/
*.pyc
/data/messages.jsonl
.DS_Store

# Local Cloudflare/Wrangler cache
.wrangler/
node_modules/

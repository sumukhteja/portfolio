# Cloudflare Pages already serves resume.html at the clean path /resume and
# 308s /resume.html to it. Never add a rule pointing /resume back at the .html
# file — the two redirects chase each other and the browser gives up.
/live  /resume  301
/cv    /resume  301
